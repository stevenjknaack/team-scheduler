version: '3.8'
services:
  db2:
    image: mysql/mysql-server:latest
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: ILoveDocker88*
      MYSQL_DATABASE: 10stars
      MYSQL_ROOT_HOST: '%'
    volumes:
      - 10stars:/var/lib/mysql
    ports:
      - "63306:3306"
volumes:
  10stars:
